apiVersion: v1
kind: Service
metadata:
  name: discourse-web
  namespace: discourse
  labels:
    app: discourse
    component: web
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: discourse
    component: web
