# PLACEHOLDER Secret — documents the name/key shape the web Deployment and migration Job
# reference. REPLACE before deploying (e.g. a SOPS-encrypted Secret / external secret store).
# Do NOT commit real credentials.
apiVersion: v1
kind: Secret
metadata:
  name: discourse-secrets
  namespace: discourse
type: Opaque
stringData:
  db-password: "REPLACE_ME"
  smtp-password: "REPLACE_ME"
  s3-access-key-id: "REPLACE_ME"
  s3-secret-access-key: "REPLACE_ME"
